<%= form_with(model: company_aproval, local: true) do |form| %>
  <% if company_aproval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_aproval.errors.count, "error") %> prohibited this company_aproval from being saved:</h2>

      <ul>
      <% company_aproval.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :admin_id %>
    <%= form.number_field :admin_id %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
